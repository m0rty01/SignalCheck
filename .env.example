# NewsAPI (optional — enables cross-source corroboration)
NEWS_API_KEY=your_newsapi_key_here

# HuggingFace token (free read-only token from huggingface.co/settings/tokens)
# Without this, HF Inference API calls still work but hit a lower rate limit
HF_TOKEN=hf_your_token_here

# Supabase (optional — if not set, feedback is logged to server/data/feedback.jsonl)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key

# Express port (Render sets this automatically)
PORT=3001

# ── Local Python ML service (optional, only needed if running ml-service locally) ──
# ML_SERVICE_URL=http://localhost:3002
