{
    "name": "signalcheck",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
        "dev:server": "node server/index.js",
        "dev:client": "cd client && npm run dev",
        "dev:ml": "cd ml-service && source venv/bin/activate && python main.py",
        "build": "cd client && npm run build",
        "render:build": "npm install && cd client && npm install && npm run build",
        "start": "NODE_ENV=production node server/index.js",
        "install:all": "npm install && cd client && npm install",
        "install:ml": "cd ml-service && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.39.7",
        "cheerio": "^1.0.0",
        "concurrently": "^8.2.2",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "node-fetch": "^3.3.2"
    }
}